# Gap List — honestly, what's still foggy after Week 1

This list is the actual point of Day 7 — not a formality. Whatever's
written here becomes the start of the Week 2 study plan.

Format: **Gap** → **Why it matters** → **Action for Week 2**

---

### 1. `[✏️ your gap here]`

*Example shape (edit or replace):* "I'm not fully confident about when
`HAVING` is needed vs. just adding another `WHERE` condition, without
checking docs."

**Why it matters:** `[✏️]`

**Action:** `[✏️ re-read a section, rebuild an exercise, ask in a
community, etc.]`

---

### 2. `[✏️ your gap here]`

*Example shape:* "I copy the same `.gitignore` pattern into every project
without being sure what every single line actually blocks."

**Why it matters:** `[✏️]`

**Action:** `[✏️]`

---

### 3. `[✏️ your gap here]`

*Example shape:* "I made `transactions.amount` nullable in Day 5 to avoid
losing a row during migration — I'm not 100% sure that was the right
trade-off vs. keeping `NOT NULL` and handling missing amounts a different
way (e.g. a separate staging table)."

**Why it matters:** `[✏️]`

**Action:** `[✏️]`

---

## Self-test (before filling this in)

Try explaining each of these out loud, no notes, no docs open:

- Why do `.groupby()` and `GROUP BY` do the same thing?
- Why does normalization reduce redundancy — with a concrete example?
- Why does a foreign key matter, practically?
- Why does a meaningful commit message matter 6 months from now?

Whichever one you hesitate on — that's a gap. Write it above.
